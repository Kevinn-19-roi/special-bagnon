import Link from "next/link";
import { adminModules } from "@/lib/admin";
export function AdminShell({ children }: { children: React.ReactNode }) { return <div className="admin-grid bg-paper"><aside className="border-r border-ink/10 bg-asphalt p-5 text-paper md:sticky md:top-0 md:h-screen"><Link href="/admin" className="focus-ring block text-sm font-semibold tracking-caps">BAGNON STREET ADMIN</Link><nav className="mt-8 grid gap-1" aria-label="Admin navigation">{adminModules.map((item) => <Link href={item.href} key={item.href} className="focus-ring flex items-center justify-between px-3 py-2 text-sm text-paper/75 transition hover:bg-paper/10 hover:text-paper"><span>{item.label}</span><span className="text-xs text-paper/45">{item.metric}</span></Link>)}</nav></aside><main className="min-w-0 p-5 md:p-8">{children}</main></div>; }
